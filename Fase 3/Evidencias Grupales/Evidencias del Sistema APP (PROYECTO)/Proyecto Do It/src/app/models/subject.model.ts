export interface Subject {
    id?: string; // Este campo es opcional si se genera automáticamente
    name: string; // El nombre de la asignatura
    description: string; // El nombre de la asignatura
}
